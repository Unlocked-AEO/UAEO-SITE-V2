{
  "name": "unlocked-aeo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=@unlocked/web",
    "dev:api": "npm run dev --workspace=@unlocked/api",
    "build:web": "npm run build --workspace=@unlocked/web",
    "build:api": "npm run build --workspace=@unlocked/api",
    "lint:web": "npm run lint --workspace=@unlocked/web"
  }
}
